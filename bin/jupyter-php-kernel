#!/bin/bash
PHP_BIN=${PHP_BIN:-php}

# Optionen parsen
# Optional: eine eigene Option für die PHP-Version hinzufügen
while [ "${1#--php=}" != "$1" ]; do
    PHP_BIN="${1#*=}"
    shift
done

# Den Rest der Argumente weiterreichen
"$PHP_BIN" "$(dirname "$0")/jupyter-php-kernel.php" "$@"
